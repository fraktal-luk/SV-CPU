; Unaligned access, loaded from previous overlapping store
$start
	
	add_h r10, r0, 0xFE
	shl_i r20, r10, -8	; 8 right
	sti_i r10, r0, 64

	sys_sync

		;ja $check

	ldi_i r11, r0, 62 ; Load from location shifted 2b left

	add_i r12, r11, -0xFE
	jnz_i r12, $error

	ldi_i r11, r0, 63 ; Load from location shifted 1b left

	sub_r r12, r11, r20
	jnz_i r12, $error

$check
	sys_dbcall
$error
    sys_error
